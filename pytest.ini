[pytest]
testpaths = tests
markers =
    gpu: touches the accelerator via a real model init/forward; run with care while training owns the GPU
    slow: real-model init/forward/train-step suites (minutes); `pytest -m "not slow"` runs the quick suite (~15s) for changes that don't touch them
addopts = -q --durations=10
